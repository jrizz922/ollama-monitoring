process_names:
  # Specific pattern for Ollama with correct path
  - name: "ollama"
    cmdline:
      - '.*/local/bin/ollama.*'
      - '.*ollama.*'

  # Docker processes
  - name: "docker"
    cmdline:
      - '.*docker.*'
      - 'dockerd.*'