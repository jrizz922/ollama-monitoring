process_names:
  - name: "{{.Comm}}"
    cmdline:
      - '.+ollama.+'

  - name: "{{.Comm}}"
    cmdline:
      - '.+open-webui.+'

  - name: "{{.Comm}}"
    cmdline:
      - '.+docker.+'
      - 'dockerd.+'