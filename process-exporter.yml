process_names:
  # More general pattern to catch Ollama regardless of exact process name
  - name: "ollama"
    cmdline:
      - '.*ollama.*'

  # Docker processes
  - name: "docker"
    cmdline:
      - '.*docker.*'
      - 'dockerd.*'